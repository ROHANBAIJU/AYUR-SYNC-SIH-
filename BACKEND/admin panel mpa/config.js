// Runtime configuration for Admin MPA (deployed on Netlify or any static host)
// This makes all admin pages call your deployed API on Render.
// Safe to commit; it contains only a public URL.

window.API_BASE_URL = 'https://ayur-sync-sih.onrender.com/api';
